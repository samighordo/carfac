<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="carfacApplicatie.lijst">
    <ContentPage.Content>
        <StackLayout>

            <Grid>
                <Label Text="resultaten" TextColor="#0081AB" Margin="10" FontSize="30" Grid.Row="0" Grid.Column="0"></Label>

                <Image Source="sort.png" WidthRequest="70" HorizontalOptions="End" Margin="0,10,10,0" Grid.Row="0" Grid.Column="1">
                    <Image.GestureRecognizers>

                        <TapGestureRecognizer Tapped="toon_popup"></TapGestureRecognizer>

                    </Image.GestureRecognizers>
                </Image>
            </Grid>

            <ListView
                x:Name="resultaatLijst" ItemTapped="item_clicked">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextCell Text="{Binding naam}"
                                  Detail="{Binding nummer}"
                                  TextColor="Black"
                                  ></TextCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

        </StackLayout>
    </ContentPage.Content>
</ContentPage>